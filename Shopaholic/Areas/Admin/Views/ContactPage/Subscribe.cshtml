@model List<Subscribe>
@{
    ViewData["Title"] = "Subscribe";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="container">

    <!--About Us-->
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title ml-2">Subscribes</h4>
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>
                                        Email
                                    </th>
                                    <th>
                                        Added Date
                                    </th>
                                    <th>
                                    </th>

                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                       
                                        <td>
                                            @item.Email
                                        </td>
                                        <td style="white-space: nowrap">
                                            @item.AddedDate.ToString("dd.MM.yyyy")
                                        </td>
                                        <td>
                                            <a onclick="return confirm('Are you sure?')" asp-area="admin" asp-controller="contactpage" asp-action="DeleteSubscribe" asp-route-id="@item.Id" class="btn btn-danger" title="delete"><i class="mdi mdi-delete"></i></a>
                                        </td>
                                    </tr>

                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>


